Database Idee.

Tables

users , is Parent
 	has
 	ID, P.K. , is UUID
 	email, Unique, Not Null, Is Varchar
 	password Not Null, Is Varchar
 	fullName Not Null, is Varchar
    birthday is Date
    photo is Varchar
    role is Enum
    isVerified is Boolean
    createdAt is TIMESTAMP

students, is child
    has
    userId is UUID, referenc users.id
    enrollmentNummer  Unique , is Long

professors , is child
     has
     userId is UUID, referenc users.id user
     personalEmail Unique, is Varchar
     cv is Varchar

admin, is Child
    has
    userId is UUID, referenc users.id

classes
    has
    ID p.k. is UUID
    name not null, is Varchar
    price not null, is Decimal
    professorId is UUID, referenc professors.userId

enrollments (The join Table)
    has
    ID p.k. is UUID,
    studentId is UUID, referenc students.userId
    classes is UUID, referenc classes.ID
    status is Enum
    enrolledAT is TIMESTAMP



